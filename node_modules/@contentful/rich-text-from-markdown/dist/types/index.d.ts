import { Document, Node } from '@contentful/rich-text-types';
import { MarkdownNode } from './types';
export type FallbackResolver = (mdNode: MarkdownNode) => Promise<Node | Node[] | null>;
export declare function richTextFromMarkdown(md: string, fallback?: FallbackResolver): Promise<Document>;
